import React from 'react';
import { StyledHeader, StyledLogo } from '../Styled-Components';

const Header = () => 
(
    <StyledHeader>
        <StyledLogo />
    </StyledHeader>
);

export default Header;